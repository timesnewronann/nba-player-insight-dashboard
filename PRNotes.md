## Summary

- configured Spring Boot to connect to the local PostgreSQL database
- added the first JPA entity for the `players` table
- created a `PlayerRepository` using Spring Data JPA
- added the first REST controller endpoint at `GET /api/players`
- verified backend JSON responses against loaded NBA player data
